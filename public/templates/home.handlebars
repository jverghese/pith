<div class="home-view">

  <div class="row-fluid">
    <div class="span12">
      <h1>{{title}}</h1>
      <div class="alert fade in bootstrap-plugin-test-alert">
        If you can close this, the bootstrap plugins are working!
        <button type="button" class="close">×</button>
      </div>
    </div>
  </div>

  <div class="row-fluid">
    <a href="#" class="add-new">add new</a>
    <div class="span6">
      <ul>
      {{#each hellos}}
        <li>
          {{this.text}}
          {{#ifequal this.text "hello"}}(default){{/ifequal}}
        </li>
      {{/each}}
      </ul>
    </div>
  </div>

</div>
